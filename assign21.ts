interface item {
      name : string
      price : number

}
//two object
const bat: item ={
      name: `hs`,
    price:`1000`
}
const ball: item ={
      name: `CA`
      price:`150`
}
console.log(`Bat name ${bat.name},price $${bat.price}`);
console.log(`Ball name ${ball.name},price $${ball.price}`);